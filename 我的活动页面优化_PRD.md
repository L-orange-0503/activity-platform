# 我的活动 PC 页面优化产品需求文档（PRD）

> 本文为「我的活动」新版页面的单一需求与设计基线。它承接活动广场头部的“我的活动”入口；本轮仅产出演示页面的视觉与交互方案，不涉及真实业务语义或后端流程。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 页面名称 | 我的活动 |
| 文档版本 | V1.1 |
| 文档状态 | 待评审 |
| 创建日期 | 2026-08-17 |
| 终端 | PC Web，固定 1920px 设计画布；内容版心固定 1200px |
| 页面入口 | 活动广场头部“我的活动”按钮，点击后直接进入本页演示 |
| 关联基线 | 活动广场方案 B（`方案B-设计说明.md`）及其共用组件规范 |

### 1.1 范围

本期包含：搜索、三个个人活动 Tab、活动列表卡片、按 Tab 显示的操作按钮、演示所需的加载/空状态及基础无障碍。

本期不包含：活动详情页重做、实际登录、权限、接口、报名/签到/提交作品/填写表单/评价的实际流程、活动排序规则变更、任何响应式布局、移动端、推荐/筛选系统。

### 1.2 事实、待确认与设计建议

| 分类 | 内容 |
|---|---|
| 已确认 | 页面包含搜索、“我报名的 / 我收藏的 / 我管理的”三个 Tab；卡片包含封面、状态、名称、时间、地点；状态文案为未开始、进行中、已结束、报名审核中。 |
| 已确认 | 我报名的可涉及报名信息、去签到、提交作品、去填表、活动评价等；我收藏的操作为取消收藏；我管理的操作为删除。 |
| 已确认 | 用户从活动广场顶部“我的活动”直接进入本页演示；本页沿用活动广场方案 B 的白色表面、克制机构蓝、细分隔线与信息分层语言。 |
| 已确认（旧稿观察） | 旧稿是居中的窄列表，含“添加”、两个 Tab（我报名的、我管理的）、搜索和卡片右侧操作；新版字段与 Tab 已发生变化。 |
| 已确认 | “报名审核中”属于活动状态，和未开始、进行中、已结束同级展示。 |
| 已确认 | 三个 Tab 的数据归属不互斥；取消收藏或删除成功后，该活动从当前 Tab 列表即时移除。 |
| 设计建议 | 新版采用固定 1200px 居中版心、单列横向活动卡片和右侧稳定操作列；默认不展示旧稿的“添加”按钮。 |

## 2. 页面基线

### 2.1 目标用户与首要任务

- 活动参与者：查找自己已报名的活动，并识别当前待办，例如签到、提交作品、填写表单或评价。
- 活动收藏者：回看感兴趣的活动或取消收藏。
- 活动管理者：查找本人管理的活动，并删除活动。

最短成功路径为：由活动广场进入 → 在当前 Tab 搜索或浏览 → 判断卡片状态、时间、地点 → 点击详情或操作按钮 → 获得对应演示反馈。

### 2.2 当前问题

- 旧稿只有两个 Tab，无法承载新增“我收藏的”内容。
- 卡片信息、操作和状态的视觉层级较弱；右侧操作与卡片内容之间存在较大空白，连续浏览效率低。
- 旧稿的“添加”按钮未在最新模块清单中出现，容易与“我管理的”职责混淆。
- 操作按钮只表达个别示例，未定义同一报名活动在不同待办阶段的展示和禁用规则。

### 2.3 不可改变的业务约束

- 活动广场到本页为直接跳转；卡片点击后直接跳转活动详情页。
- 本页仅用于确定视觉和交互方案；不接入登录、权限、接口或真实业务操作流程。
- 三个 Tab 可包含同一活动；演示数据中的 Tab 归属、状态和操作按钮由静态数据控制。
- 空的可选字段整项隐藏；不展示“暂无”、破折号或空占位。

## 3. 单一优化方案

### 3.1 任务路径

1. 用户在活动广场点击“我的活动”，直接进入本页。
2. 页面默认打开“我报名的”，展示当前 Tab 的首屏骨架；加载成功后显示列表及结果数。
3. 用户可输入关键词，在当前 Tab 内按 Enter 或点击搜索按钮检索；清空关键词后恢复该 Tab 默认列表。
4. 用户切换 Tab，页面保留搜索词并在新 Tab 内重新筛选；搜索结果和结果数只代表当前 Tab。
5. 用户通过封面、状态、标题、时间、地点判断活动；点击任意非操作区的卡片内容，直接跳转详情页。
6. 收藏取消与删除后，活动从当前列表即时移除；演示仅给出轻量成功反馈，不处理真实流程或接口失败。

### 3.2 信息架构与字段分层

页面自上而下由以下区块构成。

| 区块 | 默认必显 | 渐进披露/条件内容 | 规则 |
|---|---|---|---|
| 页面头部 | 一级标题“我的活动”、当前 Tab 内搜索框 | 无 | 不显示返回活动广场等入口；搜索框占位为“搜索活动名称”，具有显式可访问名称。 |
| Tab 导航 | 我报名的、我收藏的、我管理的 | 可选数量徽标，仅在演示数据配置数量时显示 | 使用同级 Tab，不以颜色单独表达选中；默认我报名的。 |
| 结果反馈 | “共 N 个活动” | 搜索关键词回显、清空搜索 | N 使用当前 Tab 的演示结果数量。 |
| 活动卡片 | 封面、状态、活动名称、时间、地点、操作区 | 完整长文本、二级操作 | 点击非操作区即跳转详情；操作区独立，避免误跳转。 |
| 列表状态 | 骨架、空结果 | 取消收藏或删除后的成功提示、删除确认弹窗 | 演示不设计真实接口失败与分页加载。 |

#### 3.2.1 卡片字段规则

| 字段 | 展示方式 | 缺失与长文本规则 |
|---|---|---|
| 活动封面 | 左侧固定 16:9，默认 192×108px；状态位于封面左上 | 无封面使用平台统一占位图，预留尺寸防止加载跳动。 |
| 状态 | 文字 + 语义色标签：未开始、进行中、已结束、报名审核中 | “报名审核中”为活动状态；演示数据仅使用这四种状态。 |
| 活动名称 | 主信息，16px/24px、600 字重，最多两行 | 两行省略；原生标题提示与卡片详情提供完整文本路径。 |
| 活动时间 | 日历图标 + 开始至结束时间 | 最多两行；不得截断到无法判断日期。 |
| 活动地点 | 地点图标 + 文本 | 最多一行；为空时整行隐藏。 |
| 操作区 | 根据当前 Tab 与演示数据中的动作配置渲染 | 不渲染空按钮；本轮不设计权限、禁用或不可执行原因。 |

不把主办方、浏览量、报名人数、标签或自定义字段作为本页默认字段；如后端要求补充展示，放入详情页或作为后续评审项，避免削弱用户完成待办的效率。

### 3.3 组件与交互

#### 3.3.1 搜索

- 搜索范围为当前 Tab 的活动名称，关键词最大 100 字符；提交时去除首尾空格、连续空格合并为一个。
- 按 Enter 或点击搜索图标提交；有输入时显示“清空搜索”按钮。
- 演示中搜索在前端即时筛选；结果变化后将阅读起点回到结果区。
- 搜索无结果文案为“未找到相关活动”，辅助说明为“请调整关键词后重试”，提供“清空搜索”。

#### 3.3.2 Tab

- 使用真实 `button` 的 `tablist/tab/tabpanel` 语义，暴露 `aria-selected`；键盘左右方向键切换，Enter/空格确认。
- 默认“我报名的”；切换后展示该 Tab 的静态演示数据。
- Tab 选中态使用浅蓝底、蓝色文字、底部 2px 指示条及可访问属性共同表达；悬停、键盘聚焦与按下均有轻量反馈。
- 搜索词跨 Tab 保留；不设计分页、游标和滚动位置恢复。

#### 3.3.3 卡片与详情入口

- 卡片除右侧操作区外的任意区域均是详情入口，点击后直接跳转详情页；操作区不得嵌套在详情入口中。
- 卡片悬停/键盘聚焦仅增强边框与极轻阴影，不使用上浮、缩放或封面放大；焦点环 2px 且清晰可见。
- 不额外显示“查看活动详情”入口或悬停文案；键盘聚焦可直接按 Enter 进入详情。

#### 3.3.4 按 Tab 的操作区

| Tab | 默认操作 | 条件操作 | 反馈与安全规则 |
|---|---|---|---|
| 我报名的 | “报名信息”作为次级文字按钮 | 演示数据为当前待办配置一个主按钮：去签到、提交作品、去填表或活动评价等 | 仅展示按钮外观和信息层级；不执行真实流程。报名审核中展示报名信息和审核说明，不展示待办主按钮。 |
| 我收藏的 | “取消收藏”次级/描边按钮 | 无 | 点击后立即从当前列表移除，并提示“已取消收藏”。 |
| 我管理的 | “删除”危险样式文字按钮 | 无 | 点击后打开确认弹窗；确认后立即从当前列表移除，并提示“已删除活动”。 |

“报名信息”及各待办操作在本轮仅作为可点击演示按钮，不需要确定跳转、抽屉、弹窗或真实流程。

#### 3.3.5 主要状态

| 组件/状态 | 规范 |
|---|---|
| 默认 | 白色表面、1px 低对比边框、可读文字层级；主要操作使用机构蓝。 |
| 悬停/聚焦/按下 | 悬停增强浅色背景或边框；聚焦有 2px 焦点环；按下降低按钮背景明度，不产生位移。 |
| 禁用 | 演示不展示不可执行按钮；若后续补充，需降低对比度、不可点击并显示原因。 |
| 首次加载 | 头部、Tab 保持可用，结果区显示 4 条横向卡片骨架，含封面、三行文字和操作占位。 |
| Tab/搜索刷新 | 演示中即时切换/筛选；无请求等待状态，不让页面高度明显塌陷。 |
| 空列表 | 我报名的：“暂未报名活动”；我收藏的：“暂未收藏活动”；我管理的：“暂无可管理活动”。搜索条件存在时优先显示搜索空结果。 |
| 操作成功 | 取消收藏或确认删除后立即移除当前卡片，并展示轻量成功提示。 |

### 3.4 视觉与固定布局

#### 3.4.1 视觉语言

- 画布沿用活动广场方案 B：页面背景 `#F8FAFC`，白色表面、克制机构蓝、细分隔线，以及“默认决策信息优先、补充信息渐进披露”的信息层级；品牌蓝 `#1769FF`，主文字 `#172B4D`，次要文字使用低饱和蓝灰。
- 采用 8px 间距体系：页面顶部 32px，头部至 Tab 24px，Tab 至结果区 24px，卡片间距 16px；卡片内边距 16px。
- 1920px 设计画布中内容版心固定 1200px，水平居中；页面两侧为稳定留白，不采用活动广场列表页的 72px 全宽内容区。
- 页面标题 24px/32px、600 字重；Tab/正文 14px/22px；卡片标题 16px/24px、600 字重；状态标签高 24px，圆角 6px；输入和按钮高 40px，圆角 8px；卡片圆角 12px。
- 状态颜色必须同时带文字：未开始为蓝色系、进行中为绿色系、已结束为中性灰、报名审核中为橙色系；颜色对比满足 WCAG 2.1 AA。

#### 3.4.2 固定布局

- 本页只输出 1920px 画布下、1200px 固定版心的设计，不定义响应式断点、缩放规则或移动端布局。
- 页面头部采用“左侧标题、右侧搜索”的单行布局；Tab、结果数和列表与版心左右边界对齐。
- 横向卡片封面固定 192×108px，右侧操作列固定 176px，信息区占中间剩余空间。

- 卡片高度由 108px 封面与 16px 上下内边距共同决定，最小 140px；时间换行为真实内容增高，操作列垂直居中且不随相邻卡片改变位置。
- 单个标题、时间或地点的省略只作用于该字段；卡片间不得因展开或图片加载产生明显跳动。
- 动效限制在 120–180ms 的颜色、边框或透明度过渡；不使用布局位移和封面缩放。

### 3.5 演示数据配置

演示数据用于验证信息层级与交互，不代表真实接口契约。

```json
{
  "id": "activity_id",
  "title": "活动名称",
  "coverUrl": "https://...",
  "status": "not_started | ongoing | ended | registration_reviewing",
  "startTime": "2026-08-17T09:00:00+08:00",
  "endTime": "2026-08-17T18:00:00+08:00",
  "location": "活动地点",
  "relations": ["joined", "favorited"],
  "actions": [
    { "key": "registration_info", "label": "报名信息" },
    { "key": "check_in", "label": "去签到", "priority": 1 }
  ]
}
```

- 同一活动可同时包含在多个 `relations` 中；切换 Tab 时按对应关系筛选，不需要互斥处理。
- `actions` 仅控制演示中的按钮文本、主次层级与是否显示；不接入 URL、权限或执行目标。
- 点击取消收藏或确认删除后，直接修改本地演示数据，使卡片从当前 Tab 即时移除。

## 4. 实现与验收

### 4.1 改动范围/文件

- 本轮只完成 PRD，未修改运行中的 `活动广场原型`。
- 后续原型/实现建议在现有 React/Vite 项目中新增“我的活动”路由、数据适配与列表组件；复用活动广场的颜色、输入、按钮、状态标签和焦点样式 token。

### 4.2 验收画布

- 主验收：1920px 固定画布，1200px 水平居中版心。
- 不做多断点、移动端、真实登录权限、接口失败或浏览器兼容性验收。
- 输入与可访问性：鼠标、键盘 Tab/Enter/Space/方向键。

### 4.3 验收清单

- [ ] 从活动广场“我的活动”入口直接进入本页。
- [ ] 三个 Tab 完整可见，默认我报名的；同一活动可出现在多个 Tab。
- [ ] 搜索仅返回当前 Tab 的匹配活动；Enter、搜索按钮和清空操作均有效。
- [ ] 每张卡片按封面、状态、标题、时间、地点、操作区的顺序展示；可选字段为空时不留空行。
- [ ] 四种活动状态均以文字加颜色表达；“报名审核中”作为活动状态展示。
- [ ] 点击非操作区的卡片内容直接跳转详情；右侧操作不会误触详情。
- [ ] 我报名的展示报名信息/待办按钮的演示层级；我收藏的取消后即时移除；我管理的删除确认后即时移除。
- [ ] 骨架、三类空列表、搜索空结果和操作成功提示均清晰可见。
- [ ] 1920px 画布下，标题、搜索、Tab、卡片信息和操作不重叠；图片、长标题和地点缺失不造成布局抖动。
- [ ] 所有控件可键盘访问，Tab 有选中语义，图标按钮有可访问名称，焦点环可见。

### 4.4 进入设计稿前待确认项

1. 旧版“添加”按钮是否仍需保留为独立创建入口；最新需求截图未包含，当前方案不纳入。
2. “报名信息”与多个并行待办按钮在演示中使用的具体组合与排序。
3. 搜索是否需要扩展到地点、主办方或活动编号；当前方案按活动名称实现。
