# 活动广场移动端产品需求文档（PRD）

> 本文档基于活动广场 PC 端字段规范及移动端参考框架图，定义 375–414px 移动端活动广场列表页的页面结构、字段内容、筛选抽屉和交互验收规则。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品/页面名称 | 活动广场移动端 |
| 文档版本 | V1.0 |
| 文档状态 | 待评审 |
| 创建日期 | 2026-08-17 |
| 设计基准 | 414px 优先，兼容 375px、390px |
| 适用范围 | 移动 Web/内嵌 WebView 的活动广场列表页 |
| 关联文档 | `活动广场_PC端UI优化_PRD.md`、`方案B-设计说明.md`、`设计与组件规范.md` |
| 不包含范围 | 活动详情页、活动创建、报名流程、管理后台、业务接口重构 |

### 1.1 需求等级

- P0：本期必须完成，不满足则不可上线。
- P1：本期建议完成；如依赖阻塞，需在评审时确认降级方案。
- P2：体验增强项，可排入后续版本。

## 2. 项目背景与目标

PC 端活动广场已经提供搜索、7 个筛选维度、三种视图和完整活动卡片。移动端需要在有限宽度内保留活动判断所需的信息，同时将复杂筛选收纳到底部抽屉，降低首屏信息拥挤。

本期目标：

1. 用户打开页面后，可快速完成“搜索—分类浏览—筛选—查看活动”的主路径。
2. 首屏直接展示活动分类，剩余 6 个筛选维度通过“筛选”按钮打开底部抽屉。
3. 活动卡片保留 PC 端一致的业务字段，不因移动端折叠而丢失关键内容。
4. 通过单行省略、横向滚动和渐进式抽屉控制移动端信息密度。
5. 默认以 List View 列表视图呈现，并保留 List、Card Grid、Masonry 三种视图切换。

## 3. 用户与使用场景

### 3.1 目标用户

- 学生、教师和校园活动参与者。
- 图书馆、文化馆、社团等活动主办方的活动浏览用户。
- 通过移动 Web 临时查找、比较和报名活动的用户。

### 3.2 核心任务

- 浏览活动分类并快速扫读活动卡片。
- 搜索活动名称、主办方或地点。
- 使用 6 个扩展筛选维度缩小结果范围。
- 判断活动状态、时间、地点、主办方和报名热度。
- 点击卡片进入现有活动详情页。

## 4. 信息架构

移动端页面从上到下固定为：

1. 系统状态栏安全区域。
2. 页面头部：返回/页面标题/“我的”入口。
3. 搜索与三种视图分段控制器。
4. 活动分类横向滚动栏及悬浮筛选按钮。
5. 活动图文卡片列表。
6. 加载、空结果、失败和全部加载完成状态。
7. 点击筛选后的底部抽屉层。

### 4.1 默认页面 ASCII 结构

```text
┌──────────────────────────────────────┐
│ 系统状态栏：时间 / 网络 / 电量        │
├──────────────────────────────────────┤
│ ‹              活动广场        我的   │
├──────────────────────────────────────┤
│ [ 搜索活动…                 ] [列表|宫格|瀑布] │
│ [全部][能力拓展][讲座活动][阅读活动][素质提升…]│
│                                  [筛选]│
├──────────────────────────────────────┤
│ [状态] [置顶]                         │
│              活动封面                 │
│                    报名数 / 浏览量    │
│ 活动标题单行省略…                     │
│ 时间单行                              │
│ 标签单行横向省略…                     │
│ 主办方单行省略…                       │
│ 活动地点单行省略…                     │
│ 自定义字段单行省略…                   │
└──────────────────────────────────────┘
```

### 4.2 筛选抽屉 ASCII 结构

```text
┌──────────────────────────────────────┐
│        页面内容变暗，底部抽屉上滑      │
├──────────────────────────────────────┤
│                ─────                 │
│ 筛选                              ×  │
│ 我能报名                    [开关]   │
│ 举办形式：[全部][线上][线下][线上+线下]│
│ 活动状态：[全部][报名中][即将开始]…   │
│ 活动时间：[全部][今天][明天][周末]…   │
│ 活动积分：[全部][1~5][6~10]…         │
│ 区域：    [全部][333][222][111]…      │
│ 所属区域：[全部][金牛区][青羊区]…     │
├──────────────────────────────────────┤
│       [重置]              [确定]       │
└──────────────────────────────────────┘
```

## 5. 页面布局与视觉规范

### 5.1 移动端画布与安全区

| 项目 | 规则 |
|---|---|
| 设计优先宽度 | 414px |
| 兼容宽度 | 375px、390px |
| 页面左右内边距 | 16px；卡片、搜索框和分类区统一对齐 |
| 页面背景 | 浅蓝灰或现有 PC 端页面背景，卡片使用白色表面 |
| 卡片圆角 | 12px |
| 卡片间距 | 16px |
| 基础间距 | 使用 8px 体系，常用 8/12/16/24px |
| 底部安全区 | 抽屉固定操作栏需叠加 `env(safe-area-inset-bottom)` |
| 状态栏 | 使用系统安全区域，不绘制虚假的运营内容 |

### 5.2 页面头部

- 高度由系统状态栏和应用头部共同决定，应用头部建议 56px。
- 左侧显示返回图标；若页面是一级入口，可隐藏返回图标但保留布局稳定性。
- 中间显示“活动广场”，单行展示。
- 右侧显示“我的”，点击进入现有“我的活动”业务流程。
- 不显示 `EN` 或其他语言切换入口。
- 头部底部使用 1px 低对比度分隔线，不使用厚重阴影。

### 5.3 搜索与视图切换

- 搜索框和三段式视图控制器位于同一行。
- 搜索框占据剩余空间，最小宽度 0，避免在 375px 宽度下挤压视图控制器。
- 搜索占位文案为“搜索活动…”。
- 支持输入、清空、键盘 Enter 提交；移动端软键盘提交等同于搜索按钮。
- 三段式控制器顺序固定为：
  1. List View 列表视图
  2. Card Grid 卡片网格布局
  3. Masonry View 瀑布流布局
- 默认选中 List View；选中态必须同时使用背景、文字/图标颜色和语义属性表达。
- 控件点击区域不小于 40×40px；每段提供可访问名称。

### 5.4 活动分类筛选栏

活动分类是默认页面唯一常驻的筛选维度，位于搜索行下方：

- 选项示例：全部、能力拓展、讲座活动、阅读活动、素质提升活动、普法教育活动、校园清洁志愿服务活动。
- 选项横向排列，允许左滑查看后续选项，不换成多行堆叠。
- 默认选中“全部”。
- 分类项采用浅色胶囊或圆角按钮；选中态使用机构蓝。
- 右侧固定展示“筛选”按钮，覆盖在分类滚动区域最右侧上方。
- 筛选按钮右侧可增加白色渐变遮罩，提示后方仍有可滑动内容。
- 分类横滑不得带动整个页面横向滚动。

## 6. 筛选抽屉需求

### 6.1 触发与层级

- 点击常驻“筛选”按钮，从页面底部打开抽屉。
- 抽屉上方显示半透明遮罩，遮罩覆盖页面内容但不覆盖系统状态栏。
- 抽屉顶部使用圆角和拖拽指示条；支持上滑展开、下滑关闭。
- 点击遮罩、关闭按钮或系统返回键关闭抽屉。
- 打开抽屉时保留当前已选条件；关闭但未点击“确定”时恢复打开前的条件。
- 抽屉默认高度建议为视口高度的 72%–88%；内容超出时抽屉内部滚动。

### 6.2 抽屉内固定内容

抽屉内展示“我能报名”开关和剩余 6 个筛选维度。活动分类不重复出现在抽屉中。

| 顺序 | 维度 | 类型 | 默认值 |
|---:|---|---|---|
| 1 | 我能报名 | 开关 | 关闭 |
| 2 | 举办形式 | 单选 | 全部 |
| 3 | 活动状态 | 单选 | 全部 |
| 4 | 活动时间 | 快捷单选 + 日期范围 | 全部 |
| 5 | 考核积分 | 快捷单选 + 自定义区间 | 全部 |
| 6 | 区域 | 单选 | 全部 |
| 7 | 所属区域 | 单选 | 全部 |

> “我能报名”是独立资格条件，不计入 6 个业务筛选维度；业务维度为举办形式、活动状态、活动时间、考核积分、区域、所属区域。

### 6.3 筛选选项与字段

#### 举办形式

- 选项：全部、线上、线下、线上+线下。
- 单选，维度内互斥。

#### 活动状态

- 选项：全部、报名中、未开始/即将开始、进行中、已结束。
- 实际状态枚举以接口返回为准，前端不自行推导状态。

#### 活动时间

- 快捷选项：全部、今天、明天、周末、最近一周、最近两周、近一个月、近三个月、选择日期。
- 选择“选择日期”后，在本维度下方展开：

```text
从 [ 年 / 月 / 日 ]  到 [ 年 / 月 / 日 ]
```

- 开始日期不得晚于结束日期；清空后恢复“全部”。

#### 考核积分

- 快捷选项：全部、1~5、6~10、11~15、16~20、自定义。
- 选择“自定义”后，在本维度下方展开：

```text
最低 [      ]  最高 [      ] 分
```

- 仅允许非负数字；最低值不得大于最高值；允许只填写一侧。

#### 区域

- 选项由接口返回；当前原型可使用 333、222、111、666、444、555 作为测试数据。
- 单选，默认全部。

#### 所属区域

- 选项由区域接口返回；当前原型可使用金牛区、青羊区、武侯区、成华区、高新区作为测试数据。
- 单选；区域变化后清空不适用的所属区域。

### 6.4 抽屉底部操作栏

- 操作栏固定在抽屉底部，使用白色背景和顶部细分隔线。
- “重置”为小宽度次级按钮，仅清除抽屉内条件并恢复默认值。
- “确定”为大宽度主按钮，占据剩余主要宽度，点击后应用筛选并关闭抽屉。
- 重置按钮宽度建议 72–96px；确定按钮占据约 60%–70% 的操作栏宽度。
- 两个按钮高度建议 48px，满足移动端触控目标。
- 应用条件后更新结果数量，列表滚动回顶部。

## 7. 活动图文卡片

### 7.1 卡片整体规则

- 默认使用 List View 单列图文卡片列表。
- 卡片宽度为容器宽度，左右与搜索和分类区对齐。
- 封面位于卡片顶部，比例 16:9，使用 `object-fit: cover`。
- 卡片点击区域覆盖整张卡片，进入现有活动详情页。
- 卡片内不依赖悬停展示信息；所有核心字段在触屏状态下直接可见。
- 同一张卡片中，缺失的可选字段整行隐藏，不保留空占位。

### 7.2 字段定义与展示规则

| 字段 | 必填性 | 移动端位置 | 展示规则 |
|---|---|---|---|
| 活动封面 | 必填 | 卡片顶部 | 16:9；无图使用统一占位图 |
| 活动状态 | 必填 | 封面左上角 | 文字+颜色；未开始、报名中、进行中、已结束 |
| 置顶状态 | 可选 | 封面左上角，与状态并列 | 仅 `isTop=true` 时显示“置顶” |
| 活动名称 | 必填 | 封面下第一行 | 单行显示，超出使用省略号 |
| 活动时间 | 必填 | 标题下一行 | 单行显示，完整日期时间优先 |
| 活动标签 | 可选 | 时间下一行 | 单行横向排列；超出显示“+N”或省略 |
| 自定义字段 | 可选 | 标签后 | 按后台顺序展示；每个字段单行省略；空值隐藏 |
| 主办方 | 可选 | 信息区 | 格式固定为“主办方：名称”，单行省略 |
| 活动地点 | 可选 | 主办方下一行 | 格式固定为“活动地点：地点”，单行省略 |
| 报名人数/名额 | 可选 | 封面右下角 | 与浏览量同组展示；有上限时显示 `N/M` |
| 浏览量 | 可选 | 封面右下角 | 与报名人数同组展示，使用浏览图标+数值 |

### 7.3 卡片视觉顺序

```text
封面：状态 / 置顶 / 报名人数 / 浏览量
标题：活动名称（单行省略）
时间：活动起止时间（单行）
标签：活动标签（单行横向省略）
补充：自定义字段（按配置逐行显示，单行省略）
机构：主办方（单行省略）
地点：活动地点（单行省略）
```

### 7.4 封面统计叠加

- 报名人数和浏览量叠加在封面右下角。
- 统计信息背后使用从透明到深色的局部黑色渐变，不覆盖整张封面。
- 统计文字使用白色，图标使用线性图标。
- 状态与置顶标记位于左上角，不与右下角统计信息混组。
- 封面图片内容变化时，统计信息仍须具备足够对比度。

### 7.5 单行省略规则

- 活动标题：1 行，`text-overflow: ellipsis`。
- 活动时间：1 行，优先完整展示；空间不足时整体省略，不拆开日期和时间。
- 每个标签：不拆分文字；标签组整体单行横向滚动或以 `+N` 收尾。
- 主办方、活动地点、自定义字段：每个字段 1 行，超出使用省略号。
- 单行省略只截断当前字段，不允许把多个字段合并为一个不可理解的省略文本。
- 完整文本通过卡片详情页、原生 `title` 或无障碍描述提供访问路径。

## 8. 三种视图在移动端的规则

### 8.1 List View 列表视图

- 默认视图，单列全宽卡片。
- 展示全部字段，按第 7 节顺序排列。
- 推荐卡片间距 16px，封面占据卡片顶部完整宽度。

### 8.2 Card Grid 卡片网格布局

- 375–414px 默认两列，列间距 12px。
- 保留同一套字段，但每个字段按单行省略；封面仍为 16:9。
- 适用于快速浏览封面和标题；不改变筛选条件和结果顺序。

### 8.3 Masonry View 瀑布流布局

- 375–414px 默认两列，列间距 12px，卡片高度由字段实际内容决定。
- 自定义字段、主办方和地点仍渲染，但各字段单行省略。
- 不强制同行等高；不得通过随机裁切或虚假字段制造高度差。

## 9. 页面交互

### 9.1 搜索

- 点击搜索框获取焦点并唤起软键盘。
- 输入关键词后点击键盘搜索或提交按钮，刷新结果。
- 有关键词时显示清空按钮；清空后恢复当前筛选下的结果。
- 搜索过程中保留当前分类、抽屉筛选和视图状态。

### 9.2 分类选择

- 点击分类后立即更新选中态。
- 分类筛选与抽屉内 6 个维度按 AND 关系组合。
- 分类横向滚动不影响页面纵向滚动。

### 9.3 视图切换

- 切换视图不清空搜索、分类和抽屉筛选。
- 尽量保留当前首个可见活动作为滚动锚点。
- 默认选择 List View；可在本地保存用户最近一次视图选择，作为 P1。

### 9.4 卡片点击

- 点击卡片任意非筛选控件区域进入活动详情。
- 卡片内部不增加悬停依赖；触屏设备直接展示字段。
- 点击统计信息或标签时，如业务没有独立跳转规则，仍进入活动详情。

### 9.5 返回与抽屉关闭

- 系统返回键优先关闭抽屉，再返回上一页面。
- 抽屉未点击“确定”关闭时，不应用本次临时选择。
- 点击“确定”后保存条件、关闭抽屉并刷新列表。

## 10. 页面状态

### 10.1 首次加载

- 状态栏、头部、搜索和分类栏先显示。
- 卡片区域使用单列骨架卡片，模拟封面、标题、时间和信息行。

### 10.2 搜索/筛选加载

- 保留当前页面结构和已选条件。
- 列表区域显示轻量加载状态，避免页面整体闪烁。
- 结果数量和卡片列表在请求完成后同步更新。

### 10.3 空结果

- 主文案：“未找到符合条件的活动”。
- 辅助文案：“请尝试调整关键词或筛选条件”。
- 提供“清除全部条件”操作。

### 10.4 加载失败

- 首次加载失败：显示错误说明和“重新加载”。
- 继续加载失败：保留已有卡片，列表底部显示“加载失败，点击重试”。
- 不将加载失败误显示为空结果。

### 10.5 抽屉状态

- 默认关闭。
- 打开：页面内容有遮罩，抽屉从底部出现。
- 内部滚动：底部操作栏固定不随内容滚走。
- 日期/积分条件展开时，抽屉内部继续滚动，操作栏保持可见。

## 11. 数据字段与接口要求

### 11.1 活动卡片数据结构（示意）

```json
{
  "id": "activity_id",
  "title": "活动名称",
  "coverUrl": "https://...",
  "status": "not_started | registering | ongoing | ended",
  "isTop": true,
  "startTime": "2026-08-14T09:00:00+08:00",
  "endTime": "2026-08-20T18:00:00+08:00",
  "signupCount": 120,
  "signupCapacity": 200,
  "viewCount": 3560,
  "tags": ["阅读活动", "能力拓展"],
  "organizer": "主办方名称",
  "location": "活动地点",
  "customFields": [
    {
      "key": "coOrganizer",
      "label": "协办方",
      "value": "某某单位",
      "visible": true,
      "order": 1
    }
  ],
  "detailUrl": "https://..."
}
```

### 11.2 列表接口要求

- 返回活动数组、总数、下一页游标/页码和是否还有更多。
- 返回筛选枚举或提供独立筛选配置接口，避免前端硬编码可配置选项。
- 明确状态枚举、时间时区、人数上限为空时的含义。
- 自定义字段仅返回允许前台展示的字段，前端仍按 `visible` 判断。
- 相同条件和游标返回稳定顺序，避免移动端继续加载重复或遗漏。

## 12. 可访问性与性能

- 所有按钮、输入框和筛选项具备可访问名称。
- 视图切换、分类和抽屉筛选使用 `aria-pressed` 或等价选中状态。
- 抽屉使用 `role="dialog"`、`aria-modal="true"`，打开后焦点进入抽屉，关闭后回到筛选按钮。
- 状态、选中态和错误信息不能只依赖颜色表达。
- 触控目标建议不小于 40×40px；抽屉底部按钮高度不小于 48px。
- 封面图片预留固定比例，避免图片加载导致页面跳动。
- 非首屏图片懒加载；列表继续加载使用 `IntersectionObserver` 或等价机制。
- 尊重 `prefers-reduced-motion`，关闭非必要滑入和列表动画。

## 13. 埋点建议

| 事件 | 触发时机 | 关键参数 |
|---|---|---|
| `mobile_activity_search` | 提交搜索 | keyword_length、result_count |
| `mobile_activity_category_change` | 选择活动分类 | category、result_count |
| `mobile_activity_filter_open` | 打开筛选抽屉 | selected_count |
| `mobile_activity_filter_apply` | 点击确定 | dimensions、result_count |
| `mobile_activity_filter_reset` | 点击重置 | reset_scope |
| `mobile_activity_view_switch` | 切换视图 | from_view、to_view |
| `mobile_activity_card_click` | 点击活动卡片 | activity_id、position、view_mode |
| `mobile_activity_my_click` | 点击“我的” | login_status |

## 14. 验收标准

### 14.1 页面布局

- [ ] 375px、390px、414px 宽度下页面不出现横向溢出。
- [ ] 页面包含系统状态栏安全区、头部、搜索、三段式视图控制器、活动分类、筛选按钮和卡片列表。
- [ ] 头部不显示 `EN`，右侧显示“我的”。
- [ ] 活动分类支持横向左滑；筛选按钮固定在分类栏最右侧并覆盖内容。
- [ ] 默认进入 List View，卡片按单列排列。

### 14.2 筛选抽屉

- [ ] 点击筛选按钮后，底部出现带遮罩的抽屉。
- [ ] 抽屉展示“我能报名”和剩余 6 个业务维度，不重复展示活动分类。
- [ ] 选择“选择日期”后显示开始/结束日期组件。
- [ ] 选择“自定义”后显示最低/最高积分输入组件。
- [ ] 点击遮罩、关闭按钮或系统返回键可关闭抽屉。
- [ ] “重置”为小宽度按钮，“确定”为大宽度主按钮。
- [ ] 未点击确定关闭抽屉时，不应用临时筛选条件。

### 14.3 卡片字段

- [ ] 每张卡片包含封面、状态、置顶（如有）、标题、时间、标签、主办方、活动地点、自定义字段（如有）、报名人数/名额和浏览量。
- [ ] 报名人数与浏览量位于封面右下角，并有局部黑色渐变背景。
- [ ] 标题、时间、标签、主办方、地点和每个自定义字段均遵守单行省略规则。
- [ ] 缺失的可选字段不留空行或无意义占位。

### 14.4 三种视图

- [ ] List View、Card Grid、Masonry 三种视图均可切换。
- [ ] 切换视图不丢失搜索、分类、抽屉筛选和结果顺序。
- [ ] Card Grid 与 Masonry 在移动端默认两列，卡片不重叠、不溢出。
- [ ] Masonry 卡片高度由内容决定，不强制同行等高。

### 14.5 状态与可访问性

- [ ] 首次加载、筛选加载、空结果、首次失败、继续加载失败均有明确反馈。
- [ ] 筛选抽屉打开后焦点可控，关闭后焦点回到筛选按钮。
- [ ] 所有核心操作可通过键盘或移动端辅助技术访问。
- [ ] 状态、选中态和错误信息不只依赖颜色表达。

## 15. 待确认项

1. 移动端“我的”是否继续使用“我的活动”完整页面标题，还是仅作为入口短文案。
2. 活动状态最终使用“未开始”还是“即将开始”，需与后端枚举统一。
3. Card Grid 和 Masonry 在 375px 宽度下是否固定两列，或低于 390px 时降为单列。
4. 自定义字段在移动端是否全部展开，还是仅展示后台配置的前 N 个字段。
5. 抽屉筛选是否采用“点击确定后统一应用”，以及是否需要保留草稿条件。
6. 活动分类是否允许多选；本文按单选设计。

## 16. 版本记录

| 版本 | 日期 | 变更内容 | 状态 |
|---|---|---|---|
| V1.0 | 2026-08-17 | 基于移动端参考框架建立页面布局、字段、筛选抽屉、视图和验收规则 | 待评审 |
| V1.1 | 2026-08-20 | 追加“我的活动”移动端页面：旧版头部框架复刻、三分类分段控件、内部滚动卡片列表和操作收纳规则 | 待评审 |

## 17. 我的活动移动端补充需求

> 本节为“活动广场移动端”的独立子页面需求，不改变第 1–16 节活动广场列表、筛选抽屉及三种视图的规则。页面字段、活动状态和分类数据沿用现有 PC 端“我的活动”页面；顶部框架仅参考旧版移动端 Figma 设计图。

### 17.1 文档信息与范围

| 项目 | 规则 |
|---|---|
| 页面名称 | 我的活动（移动端） |
| 设计宽度 | 375px，一倍图基准；不以真实手机物理高度限制设计稿展示内容 |
| 设计参考 | 旧版移动端 Figma 节点 `488:20592`：仅复刻头部、搜索、分段控件及列表容器的框架样式 |
| 数据与字段基线 | 现有 PC 端“我的活动”页面：封面、活动状态、活动名称、活动时间、活动地点及分类对应操作 |
| 演示数据 | 三个分类各使用 6 张静态示例卡片，覆盖所有状态与操作收纳场景 |
| 本期不包含 | 签到、报名信息、填写表单、提交作品、评价、取消收藏、删除、卡片详情及“+”按钮的后续跳转页面或真实业务流程 |

### 17.2 已确认需求留存

以下内容为后续设计与开发的固定约束；如需修改，须在本节新增版本记录，不得在实现阶段自行调整。

1. 顶部模块严格按旧版移动端 Figma 的一倍图框架复刻：系统安全区、返回符号、居中“应用标题”、右侧“+”、搜索框、分段控件和其下分隔线均不做视觉优化或文案替换。
2. 仅顶部模块沿用旧稿；页面业务内容严格采用现有 PC 端“我的活动”页面。分段分类及顺序固定为“我报名的 / 我收藏的 / 我管理的”，不得退回旧稿中的双分类。
3. 页面宽度按 375px 设计。设计评审稿可纵向展开完整展示 6 张卡片；运行时仅活动列表区域内部纵向滚动，顶部模块不参与列表滚动。
4. 每张活动卡片只展示 PC 端已有字段：封面、状态、名称、时间、地点。不得从旧稿新增或替换业务字段。
5. 状态标签只出现一次，固定叠加在封面左上角；状态枚举为“未开始、进行中、已结束、报名审核中”。
6. 右下角固定展示“二维码扫描图标 + 去签到”悬浮按钮；不显示数量、徽标或说明文案，本期不定义点击后的跳转页面。
7. “我报名的”卡片根据配置展示操作；“我收藏的”仅展示取消收藏；“我管理的”仅展示删除。报名审核中的卡片不展示任何操作区。

### 17.3 页面结构与滚动规则

```text
┌──────────────────────────── 375px 设计画布 ────────────────────────────┐
│ 系统安全区 / ‹ / 应用标题 / +                                          │ 固定
├───────────────────────────────────────────────────────────────────────┤
│ 搜索框                                                                  │ 固定
│ [我报名的] [我收藏的] [我管理的]                                        │ 固定
├───────────────────────────────────────────────────────────────────────┤
│ 活动列表内容区                                                     ┃  │
│ 卡片 1                                                               ┃  │
│ 卡片 2                                                               ┃  │
│ 卡片 3                                                               ┃  │  内部纵向滚动
│ 卡片 4                                                               ┃  │
│ 卡片 5                                                               ┃  │
│ 卡片 6                                                               ┃  │
│                                                   [二维码图标 去签到]  │ 固定
└───────────────────────────────────────────────────────────────────────┘
```

- 页面外层不随活动列表滚动；列表容器使用独立的纵向滚动上下文。
- 设计稿可将内容区高度拉长，以完整呈现 6 张卡片；交互原型和运行页面仍须验证列表区域可独立滚动。
- 悬浮“去签到”按钮固定在页面右下角、位于内容区之上；列表底部预留不少于按钮高度加 16px 的安全留白，保证最后一张卡片不被遮挡。
- 搜索提交仅筛选当前分类；切换分类保留搜索关键词，结果和滚动位置按当前分类重新计算。

### 17.4 顶部与分段控件

#### 17.4.1 旧版头部框架复刻

- 以 Figma 节点 `488:20592` 的一倍图为唯一视觉基准；保留白色顶栏、1px 分隔线、左侧返回符号、居中“应用标题”和右侧“+”。
- 搜索框位于顶栏下方，保留旧稿的浅灰胶囊背景、居中搜索图标与“搜索”占位文案。
- 顶部“+”和返回符号在本期仅保留界面展示，不要求定义后续跳转页面。
- WebView/浏览器实现时使用安全区占位，不伪造系统运营商、时间或电量内容；设计稿可按参考图留出同等安全区高度。

#### 17.4.2 三分类分段控件

- 分段控件延续旧稿的圆角胶囊样式、浅灰未选中态、蓝色描边/文字选中态和紧凑间距。
- 分类按现有 PC 端顺序固定为：我报名的、我收藏的、我管理的；默认选中“我报名的”。
- 375px 宽度下三个分类需完整可见，不允许换行、裁切或横向滚动。
- 选中态除颜色外，须通过边框、字重及 `aria-selected` 表达；切换时仅更新列表内容，不改变顶部高度。

### 17.5 卡片字段与视觉结构

#### 17.5.1 字段与顺序

| 顺序 | 字段 | 规则 |
|---:|---|---|
| 1 | 活动封面 | 位于卡片左侧，固定比例裁切；图片使用现有 PC 端本地活动封面。 |
| 2 | 活动状态 | 叠加于封面左上角，仅出现一次。 |
| 3 | 活动名称 | 位于封面右侧，最多两行；超出后省略。 |
| 4 | 活动时间 | 名称下方，显示开始时间至结束时间；单行省略，不拆分为无意义的日期片段。 |
| 5 | 活动地点 | 时间下方，带地点图标；最多一行，空值时整行隐藏。 |
| 6 | 操作区 | 仅在该卡片存在可执行展示操作时出现，位于卡片底部。 |

```text
┌─────────────────────────────────────────────────────────────────────┐
│ ┌────────────┐  活动名称（最多两行）                                  │
│ │    封面     │  2026.08.14 14:00 — 17:00                             │
│ │ [活动状态] │  ◉ 活动地点                                            │
│ └────────────┘ ────────────────────────────────────────────────────│
│ [更多]（条件显示）             [次级操作] [次级操作] [主操作]          │
└─────────────────────────────────────────────────────────────────────┘
```

- 本页面不展示主办方、标签、报名人数、浏览量、置顶状态或自定义字段；这些字段不属于当前 PC 端“我的活动”卡片默认内容。
- “报名审核中”是活动状态，不是操作按钮或额外字段；该状态卡片仅保留封面、状态、名称、时间和地点。
- 卡片本身是 UI/UX 展示表面，本期不新增详情跳转、详情弹窗或其他点击后的页面。

### 17.6 分类与操作收纳规则

#### 17.6.1 我报名的

| 卡片状态/操作数量 | 展示规则 |
|---|---|
| 报名审核中 | 完全隐藏操作区。 |
| 操作总数为 1–3 个 | 全部操作按钮直接展示；不出现“更多”。 |
| 操作总数超过 3 个 | 同时直接展示优先级最高的 3 个操作按钮；第 4 个及以后操作收纳至“更多”下拉菜单。 |

- 直接展示的主操作始终位于最右侧：未开始或进行中优先为“去签到”，已结束优先为“提交作品”。
- “更多”仅在操作总数大于 3 时出现，固定于操作区最左侧；它不占用“3 个可直接展示操作”的名额。
- 下拉菜单仅展示未直接露出的操作项，例如报名信息、去填表、活动评价；菜单的出现不改变卡片高度或遮挡主操作。
- 演示数据需至少覆盖以下三种报名卡片：3 个操作且无更多、4 个及以上操作且显示更多、报名审核中且无操作。

#### 17.6.2 我收藏的与我管理的

| 分类 | 卡片操作 | 移动端规则 |
|---|---|---|
| 我收藏的 | 取消收藏 | 触屏端直接展示为单个次级操作，不使用悬停前置条件，不进入“更多”。 |
| 我管理的 | 删除 | 触屏端直接展示为单个危险操作，不使用悬停前置条件，不进入“更多”。 |

- 本期仅展示操作按钮样式；取消收藏、删除、更多菜单项和“去签到”均不要求提供后续跳转页面或真实业务处理。

### 17.7 六张演示卡片与分类覆盖

每个分类默认展示 6 张来自当前 PC 演示数据的静态卡片，确保设计评审能完整核对字段和状态。可复用同一活动在多个分类中出现，不要求三类数据互斥。

| 分类 | 6 张示例覆盖 | 必须校验的状态/操作 |
|---|---|---|
| 我报名的 | 阅读经典、校园绿色行动日、心理健康讲座、传统文化体验日、青春在场、秋日校园摄影采风 | 进行中、未开始、报名审核中、已结束；无更多、含更多、无操作区。 |
| 我收藏的 | 阅读经典、校园绿色行动日、心理健康讲座、传统文化体验日、青春在场、美好社区共创工作坊 | 四种状态均可出现；每卡只显示取消收藏。 |
| 我管理的 | 阅读经典、心理健康讲座、传统文化体验日、青春在场、山河同读、秋日校园摄影采风 | 四种状态均可出现；每卡只显示删除。 |

### 17.8 悬浮去签到按钮

- 固定在页面内容区右下角，视觉为蓝色圆角胶囊。
- 内容固定为二维码扫描图标和“去签到”文字；不显示待签到数量、角标、提示文案或次级说明。
- 本期仅验收按钮位置、样式、层级和安全区避让；不设计点击后的新页面、抽屉、弹窗或签到流程。

### 17.9 验收标准

- [ ] 页面以 375px 设计宽度实现，顶部模块在样式、间距和文案上与 Figma 节点 `488:20592` 一致；顶部不使用 PC 端深蓝标题栏。
- [ ] “我报名的 / 我收藏的 / 我管理的”三段分类完整可见，默认我报名的，切换后显示各自 6 张演示卡片。
- [ ] 顶部区域固定，只有活动列表内容区可独立纵向滚动；展开的设计评审稿可完整呈现 6 张卡片。
- [ ] 每张卡片只展示封面状态、标题、时间和地点；状态只在封面左上角出现一次。
- [ ] 我报名的：操作数不超过 3 时无“更多”；超过 3 时仍展示 3 个操作按钮，剩余项进入左侧“更多”菜单；报名审核中不显示操作区。
- [ ] 我收藏的卡片仅显示取消收藏；我管理的卡片仅显示删除；二者均不进入“更多”。
- [ ] 右下角固定展示“二维码扫描图标 + 去签到”，不显示数量，也不实现后续页面。
- [ ] 不新增卡片详情、操作跳转、弹窗或真实接口流程。
