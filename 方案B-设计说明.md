# 活动广场优化方案 B：筛选与信息分层

## 定位与边界

方案 B 是活动广场的体验升级方案，重点重构“找活动”的筛选路径与卡片信息层级；不只是对方案 A 做视觉换肤。

- 方案 A：保留原有筛选结构和字段规则，以视觉统一与页面秩序提升为主。
- 方案 B：复用方案 A 的活动数据、封面素材、基础视图能力和技术骨架；独立调整筛选信息架构、结果反馈与卡片显隐规则。
- 方案 A 必须保留，方案 B 在独立工作树中实现，便于并排评审和回退。

## 筛选与结果反馈

### 默认筛选层级

1. 常用筛选常驻：举办形式、活动状态、活动分类、活动时间。
2. 扩展筛选收纳：考核积分、区域、所属区域，置于“更多筛选”入口或可展开区。
3. 结果工具栏常驻：活动总数、已选条件标签、重置、我的活动/我能报名、视图切换。

### 交互规则

- 每个维度内单选，维度之间按 AND 组合；与现有数据筛选语义一致。
- 选择后即时刷新结果；非“全部”的已选项显示为可单独删除的标签。
- “全部”仅代表默认状态，不展示为已选标签。
- 日期范围、积分等输入仅在用户选择对应条件后展开，不占用默认筛选区高度。
- 区域与所属区域联动：改变区域后，清空不适用的所属区域条件。
- 筛选区需支持键盘可达和清晰的焦点态。

## 卡片信息分层

### 默认展示：帮助快速判断是否感兴趣

- 活动状态、置顶标识、封面、标题、时间。
- 报名人数/名额、线上或线下的关键地点/资格信息。
- 对报名决策必需的其他字段。

### 悬停或键盘聚焦后补充：帮助进一步理解

- 主办方（格式为“主办方：xxxx”）、完整地点、剩余标签。
- 1–3 个自定义字段、完整浏览量。
- 如业务具备，对应的收藏、分享等轻操作。

### 信息显隐原则

- 活动时间、活动状态、线下地点或报名资格不能只在悬停态出现。
- 悬停内容必须在卡片内部浮现或以浮层呈现，不得推高整行卡片或造成 Grid 布局抖动。
- 键盘聚焦提供与悬停相同的信息；触屏设备改用明确的“更多信息”入口。
- 自定义字段默认不出现；仅在悬停/聚焦态或“更多信息”中出现。

## 视图规则

- List、Card Grid、Masonry 三种视图均保留。
- Masonry 是富信息探索视图，可在默认态额外展示一行自定义字段。
- 单列卡片封面填满卡片高度，封面与右侧信息不使用竖向分隔线。
- 瀑布流至少提供 8 条活动数据。

## 已确认的视觉与内容约束

- 1920px-first 桌面端；白色表面、克制的机构蓝、细分隔线、72px 页面左右留白、24px 布局间距。
- 搜索位于最左侧，需更醒目，可使用蓝色强调；不显示“活动广场”页面标题。
- “我的活动”按钮使用左侧“我的”语义图标，不使用右箭头。
- 封面使用语义匹配的 Unsplash 本地占位图，图片宽度不超过 900px。

## 待评审项

- “我的活动”与“我能报名”的最终命名及是否同时保留。
- 更多筛选的默认展开策略（完全收起或保留已选项所在行）。
- 卡片悬停区内是否提供收藏、分享等轻操作。
