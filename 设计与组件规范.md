# 活动广场：设计与组件共用规范

> 适用范围：方案 A（视觉优化）与方案 B（筛选与信息分层优化）。本规范约束两案共享的视觉语言、组件形态与基础交互；不规定筛选信息架构与卡片字段显隐策略。

## 1. 设计基线

- **终端与画布**：1920px-first 桌面端；页面左右留白 72px，主要内容分区间距 24px。
- **整体气质**：白色表面、克制的机构蓝、清晰但不过度的层级；避免大面积高饱和色块与厚重阴影。
- **信息原则**：优先让用户识别活动、判断时间与报名条件；装饰性信息不得削弱任务主线。
- **响应式基线**：小屏时优先保证搜索、筛选、结果数与卡片主信息可用；悬停专属内容必须有触屏等价入口。

## 2. 设计令牌

| 类别 | 规范 |
| --- | --- |
| 品牌强调色 | 使用克制的机构蓝作为主操作、选中态和搜索强调；不将其用于整块背景铺陈。 |
| 表面与边界 | 页面和卡片以白色为主；分隔使用细、低对比边框，不使用粗重描边。 |
| 文字层级 | 标题承担首要阅读权重；时间、地点等决策信息次之；说明与统计信息使用更弱的灰阶。 |
| 间距 | 使用 8px 体系；常用组件内间距 8/12/16px，模块间距 24px。 |
| 圆角 | 卡片、输入与按钮保持同一组中等圆角，避免同屏混用尖角、胶囊和超大圆角。 |
| 阴影 | 默认弱化或不使用；卡片悬停/聚焦时可使用轻量阴影和边框变化表达可交互性。 |
| 图标 | 使用线性、语义明确的图标；图标通常置于文字左侧，不以右箭头代替“我的活动”等功能语义。 |

## 3. 页面骨架

### 3.1 顶部操作区

- 搜索位于最左侧，是页面最醒目的入口之一；可使用品牌蓝强调搜索操作。
- 不显示“活动广场”作为页面标题。
- “我的活动”使用左侧的“我的/用户”语义图标，不使用右箭头。
- 搜索、我的活动与结果区在视觉上形成“查找 → 筛选 → 浏览”的连续路径。

### 3.2 筛选与结果区

- 筛选项采用短文本选项，维度标签与选项保持稳定对齐。
- 选中态由浅品牌底色、品牌色文字或轻量边界共同表达，并须同时提供键盘焦点态。
- 结果数、重置、报名资格开关与视图切换位于结果工具栏，避免分散在卡片区。
- “共 N 个活动”下方不额外添加无信息价值的分割线。

### 3.3 结果卡片区

- 所有视图使用同一套活动状态、封面、标题、时间、标签和操作语义。
- 封面为语义匹配的本地 Unsplash 占位图，宽度不超过 900px。
- 状态、时间、地点/报名资格等影响决策的信息必须清晰可见。
- 默认卡片不展示自定义字段；展示策略由各方案的卡片规则决定。

## 4. 基础组件规范

### 4.1 搜索框

- 包含输入、清空（存在关键词时）与提交操作；提供清晰的占位提示与无障碍名称。
- 支持 Enter 提交，搜索词即时影响结果或提供明确的提交反馈。

### 4.2 按钮与图标按钮

- 主按钮使用品牌蓝，次级按钮使用中性表面与边框；禁用态应降低对比度且不可交互。
- 图标按钮必须有 `aria-label` 或等价文字说明。
- 鼠标悬停、键盘焦点、按下与禁用状态均需可区分。

### 4.3 筛选选项与已选标签

- 筛选选项是可点击的文本控件，使用 `aria-pressed` 或等价语义表达选中状态。
- 已选标签必须支持单独移除；“全部”不作为标签回显。
- 重置操作需同时清空筛选、关键词与资格开关。

### 4.4 活动卡片

- 固定的主信息顺序：状态/标识 → 封面 → 标题 → 时间 → 报名与地点等决策信息。
- 单列视图中封面填满卡片高度，封面与文字区之间不添加竖向分割线。
- 卡片必须可键盘聚焦和打开详情；悬停态与聚焦态提供等价反馈。
- 主办方字段一律使用“主办方：xxxx”格式。

### 4.5 状态与标签

- 活动状态使用颜色加文字共同表达，不能只依赖色彩。
- 标签数量受控，避免挤压标题与时间；超出信息可放入详情或渐进披露层。

### 4.6 视图切换与详情弹窗

- List、Card Grid、Masonry 使用同一组视图切换控件，并提供当前视图的 `aria-pressed` 状态。
- 详情弹窗需标注 `role="dialog"`、`aria-modal="true"`，支持关闭与背景点击关闭。

## 5. 交互与可访问性

- 所有鼠标悬停出现的信息必须在键盘聚焦时可得；触屏端以“更多信息”或展开操作提供等价访问。
- 筛选、搜索、视图切换和卡片打开均可通过键盘完成。
- 切换筛选条件时，结果数与已选状态应即时更新，并保留清晰的变化反馈。
- 卡片的补充内容不得导致同一行的其他卡片跳动；使用内部展开、叠层或稳定高度策略。

## 6. 两案差异边界

| 范畴 | 方案 A | 方案 B |
| --- | --- | --- |
| 筛选结构 | 保留七个维度平铺及原有顺序。 | 可将常用筛选常驻、扩展筛选收纳，并增加已选条件回显。 |
| 筛选语义 | 保持现有筛选逻辑。 | 保持维度内单选、维度间 AND；区域与所属区域可联动。 |
| 卡片信息 | 以既有字段为主，仅优化视觉秩序。 | 采用“默认决策信息 + 悬停/聚焦补充信息”；自定义字段默认隐藏。 |
| 瀑布流 | 与现有字段规则一致。 | 可作为富信息探索视图，默认增加一行补充信息。 |

## 7. 使用方式

- 方案 A 和方案 B 的页面代码均以本规范为共同基准。
- 新增或改动共享组件时，先更新本规范，再同步两案实现。
- 只属于某一方案的筛选、字段显隐和实验逻辑，应记录在各自方案说明中，不反向改写本规范。
