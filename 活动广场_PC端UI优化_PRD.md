# 活动广场 PC 端 UI 优化产品需求文档（PRD）

> 本文件是“活动广场 PC 端 UI 优化”项目的长期需求基线（Single Source of Truth）。后续无论是否新开对话，继续本项目时均应优先读取本文件；需求变更须同步更新文档版本与变更记录。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品/页面名称 | 活动广场 |
| 线上地址 | <https://hd.chaoxing.com/?marketId=5376> |
| 文档版本 | V1.2 |
| 文档状态 | 需求基线，待评审 |
| 创建日期 | 2026-08-14 |
| 标准设计画布 | PC Web，1920px 宽 |
| 设计基准 | 页面左右固定 72px 内边距，内容区宽 1776px |
| 适用范围 | 活动广场列表页 PC 端 UI、交互、响应式及状态设计 |
| 不包含范围 | 移动端、活动详情页、活动创建/管理后台、我的活动内页、后端活动业务逻辑重构 |

### 1.1 变更记录

| 版本 | 日期 | 变更内容 | 状态 |
|---|---|---|---|
| V1.2 | 2026-08-14 | 基于线上两种现有布局补充跨视图字段分配、信息密度、截断、对齐和响应式规则 | 待评审 |
| V1.1 | 2026-08-14 | 明确三种正式布局术语：List View、Card Grid、Masonry View，并统一切换与验收口径 | 待评审 |
| V1.0 | 2026-08-14 | 建立页面结构、卡片字段、三种视图、响应式、状态、可访问性和验收基线 | 待评审 |

### 1.2 需求等级定义

- P0：本期必须完成，不满足则不可上线。
- P1：本期应完成；如受依赖阻塞，须在评审时明确降级方案。
- P2：体验增强项，可排入后续版本。

## 2. 项目背景

活动广场面向跨教育阶段和公共文化场景的用户，用于发现、筛选、浏览并进入活动详情。现有页面已具备搜索、7 维筛选、List View（列表视图）/Card Grid（卡片网格布局）、“我的活动”入口、报名资格筛选以及活动卡片列表。本次优化以信息查找效率、内容可读性、界面一致性和超宽屏适配为核心，同时新增 Masonry View（瀑布流布局）。

线上页面于 2026-08-14 核对到的现状：

- 搜索占位文案为“请输入关键词”。
- 7 个筛选维度为：举办形式、活动状态、活动分类、活动时间、考核积分、区域、所属区域。
- 已有两种布局：List View（列表视图 / 列表流 / 左图右文列表）和 Card Grid（卡片网格布局 / 网格卡片流）。
- 本期新增第三种布局：Masonry View（瀑布流布局 / 瀑布卡片流）。
- 结果工具栏包含结果数量、“我能报名”复选项和视图切换。
- 活动列表当前采用滚动触底继续加载。
- 当前第 8 条“全部字段”可验证置顶、状态、报名人数/名额、浏览量、标签、自定义字段、主办方及地点等完整信息。

## 3. 用户与使用场景

### 3.1 目标用户

1. 基础教育用户：中小学教师、学生及相关活动组织者。
2. 职业院校用户：职业院校师生及校园文化活动管理者。
3. 高等院校用户：高校师生、社团成员、活动组织者。
4. 公共文化单位用户：图书馆、文化馆等机构工作人员与服务对象。
5. 社会群众：通过公开活动入口查找并参与文化、讲座、阅读、赛事等活动的用户。

### 3.2 核心任务

- 快速搜索已知名称或关键词的活动。
- 通过多个维度缩小活动范围。
- 快速判断活动状态、时间、报名热度、地点和主办方。
- 在不同信息密度下切换列表、宫格或瀑布流视图。
- 进入活动详情并完成后续报名或浏览。
- 已登录用户进入“我的活动”查看个人相关活动。

### 3.3 典型使用环境

- 桌面浏览器、笔记本电脑和大屏显示器。
- 鼠标/触控板为主，同时支持键盘操作和触屏 PC。
- 常见宽度覆盖 1024px 至 4K；本期不提供移动端专门布局。

## 4. 产品目标与成功标准

### 4.1 产品目标

1. 建立清晰的页面层级，使用户能快速完成“搜索/筛选—浏览—进入详情”。
2. 在复杂字段和不同内容长度下保持卡片可扫描、可比较。
3. 新增瀑布流视图，提高包含自定义字段或长文本卡片的空间利用率。
4. 严格按指定断点实现 3/4/5/6 列响应式布局。
5. 完善加载、空结果、异常、无权限等状态，保证页面可恢复、可理解。
6. 满足 PC Web 基础可访问性、性能和主流浏览器兼容要求。

### 4.2 建议数据指标

以下指标需在上线前补齐现状基线，目标值可在评审后调整：

- 活动卡片点击率。
- 搜索后进入活动详情的转化率。
- 使用筛选后进入活动详情的转化率。
- 三种视图的使用占比及切换率。
- 搜索零结果率。
- 首屏加载成功率与列表继续加载失败率。
- “我的活动”入口点击率。

### 4.3 非目标

- 不改变活动创建、报名、审核、签到等后端业务流程。
- 不新增排序、推荐、个性化算法或活动运营位。
- 不重做活动详情页和“我的活动”内页。
- 不在本期建立移动端信息架构。
- 不改变后台自定义字段的配置机制，只规范前台展示。

## 5. 信息架构与页面结构

页面自上而下由以下区域构成：

1. 页面头部：页面标题“活动广场”、搜索、我的活动入口。
2. 筛选区：7 个筛选维度及重置能力。
3. 结果工具栏：结果数量、当前筛选摘要（可选增强）、“我能报名”、三种视图切换。
4. 活动结果区：活动卡片列表。
5. 列表状态区：骨架屏、加载更多、加载失败、空结果、全部加载完成。

### 5.1 1920px 标准画布布局

- 浏览器/设计画布宽：1920px。
- 页面左右内边距：固定 72px。
- 主内容区宽：1776px。
- 页面顶部间距：32px。
- 区域间垂直间距：24px；结果工具栏与列表间距可为 16px。
- 宫格及瀑布流列间距：24px。
- 宫格行间距：24px；瀑布流卡片纵向间距：24px。
- 所有页面区域左、右边界统一对齐到 72px 内容基线。

### 5.2 三种视图布局的正式术语

为避免产品、设计、开发和测试对“列表”“卡片流”“瀑布流”的理解不一致，本项目统一使用以下名称：

| 英文正式名称 | 中文正式名称 | 文档允许简称 | 核心特征 | 当前状态 |
|---|---|---|---|---|
| List View | 列表视图（列表流 / 左图右文列表） | 列表视图、List | 单列、左侧封面、右侧文字信息，纵向逐条排列 | 已支持，本期优化 |
| Card Grid | 卡片网格布局（网格卡片流） | 卡片网格、宫格、Grid | 多列等宽网格；同一行按最高卡片统一行高 | 已支持，本期优化 |
| Masonry View | 瀑布流布局（瀑布卡片流） | 瀑布流、Masonry | 多列等宽、卡片高度由内容决定，各列独立向下紧凑排列 | 本期新增 |

- “视图切换”是同一活动结果集的展示方式切换，不改变搜索条件、筛选条件、数据顺序或活动数量。
- 后续原型、视觉稿、代码命名、测试用例和埋点优先使用以上英文正式名称与中文正式名称。
- 本文中的“宫格视图”均指 Card Grid；“瀑布流视图”均指 Masonry View。

## 6. 功能需求

### 6.1 搜索（P0）

#### 6.1.1 展示

- 搜索框位于页面头部，建议宽 360–480px、高 40px。
- 占位文案：“搜索活动名称”。如后端支持多字段检索，可改为“搜索活动名称、主办方或地点”。
- 包含搜索图标；有输入时显示一键清空按钮。
- 搜索框必须有可访问名称，不得仅依赖占位文案。

#### 6.1.2 交互

- 用户按 Enter 或点击搜索按钮后执行搜索。
- 自动去除关键词首尾空格；连续空格按一个空格处理。
- 空关键词提交等同于清除搜索条件并恢复当前筛选下的全部结果。
- 搜索过程中保留已选筛选项和当前视图。
- 新搜索发起后滚动回结果区顶部并重新加载第一页/首批数据。
- 关键词建议限制 100 个字符；超出时不继续输入并提供明确提示。

#### 6.1.3 搜索范围

- P0：至少支持活动名称模糊匹配。
- P1：支持活动标签、主办方、活动地点匹配。
- 自定义字段是否纳入搜索由后台能力决定，不作为本期前端上线阻塞项。

### 6.2 筛选栏（P0）

#### 6.2.1 筛选维度

| 维度 | 当前可见选项/规则 | 目标规则 |
|---|---|---|
| 举办形式 | 全部、线上、线下、线上+线下 | 单选；选项由枚举或后台配置返回 |
| 活动状态 | 当前为全部、即将开始、进行中、已结束 | 目标状态命名见 8.2；单选 |
| 活动分类 | 能力拓展、讲座活动、阅读活动等 | 单选；选项动态返回，支持换行 |
| 活动时间 | 全部、今天、明天、周末、最近一周、最近两周、近一个月、近三个月、选择日期 | 单选快捷项；“选择日期”使用日期范围 |
| 考核积分 | 全部、1~5、6~10、11~15、16~20、自定义 | 单选快捷项；“自定义”支持最小值/最大值 |
| 区域 | 当前为后台测试数据 | 单选；数据由后台返回 |
| 所属区域 | 当前为行政区数据 | 单选；数据由后台返回 |

#### 6.2.2 通用规则

- 每个维度默认选中“全部”。
- 同一维度单选，不同维度之间按 AND 关系组合。
- 点击选项后立即刷新结果，无需额外“确定”按钮；日期范围和自定义积分在完成输入后应用。
- 当前选项必须具有明显但克制的选中态，且不能仅依赖颜色表达。
- 选项过多时允许自然换行；维度名称列宽保持一致，选项区占据剩余空间。
- 提供“重置筛选”操作；仅在存在非默认筛选时可用。
- 重置筛选不清空搜索关键词；如需一键清空全部条件，应使用独立的“清除全部条件”。
- 筛选变化后滚动回结果区顶部并重置列表加载游标。
- 接口请求期间禁用重复提交，但不锁死整个页面。

#### 6.2.3 日期范围

- 支持开始日期和结束日期；开始日期不得晚于结束日期。
- 默认按自然日计算，开始为 00:00:00，结束为 23:59:59。
- 时间范围无效时在控件附近提示，不发起请求。
- 清空日期范围后恢复“全部”。

#### 6.2.4 自定义积分

- 仅允许非负数字。
- 最小值不得大于最大值。
- 支持只填最小值或只填最大值。
- 输入无效时就地提示，不发起请求。

### 6.3 结果工具栏（P0）

- 左侧显示“共 N 个活动”，使用后端返回的总数，不以当前已加载卡片数代替。
- 右侧依次显示“我能报名”和视图切换。
- “我能报名”为独立筛选条件；选中后仅展示当前用户具备报名资格的活动。
- 未登录用户点击“我能报名”时，进入现有登录流程；登录完成后应返回原页面并恢复原搜索/筛选/视图状态。
- 当工具栏空间不足时允许左右两组换行，但视图切换三个按钮不得拆散。
- P1：在结果数量旁展示已选条件摘要，并支持逐项删除。

### 6.4 视图切换（P0）

#### 6.4.1 视图类型与顺序

1. **List View 列表视图（列表流 / 左图右文列表）**：现有能力，始终单列。
2. **Card Grid 卡片网格布局（网格卡片流）**：现有能力，按响应式规则展示 3/4/5/6 列。
3. **Masonry View 瀑布流布局（瀑布卡片流）**：本期新增，按响应式规则展示 3/4/5/6 列。

#### 6.4.2 控件规范

- 使用一个三段式视图切换控件，每个按钮建议 40×40px，最小可点击区域不得低于 36×36px。
- 三个按钮分别提供清晰图标与悬浮提示：“列表视图（左图右文）”“卡片网格布局”“瀑布流布局”。
- 按钮使用真实 `button` 元素，并提供 `aria-label` 与 `aria-pressed`。
- 选中态同时通过底色、图标色和可访问属性表达。
- 默认视图为 Card Grid 卡片网格布局。
- P1：在同一浏览器保存用户最后一次视图选择；无法读取偏好时回退 Card Grid 卡片网格布局。

#### 6.4.3 切换行为

- 切换视图不得重新发起相同条件的数据请求，除非当前视图需要不同接口字段。
- 保留搜索、筛选、“我能报名”、已加载数据和活动顺序。
- 尽量保持切换前首个可见活动作为切换后的滚动锚点，避免用户丢失浏览位置。
- 切换过程不使用大幅位移动画；允许 120–180ms 的淡入或轻微过渡。
- 尊重 `prefers-reduced-motion`，用户要求减少动态效果时关闭非必要动画。

### 6.5 我的活动入口（P0）

- 位于页面头部右侧，使用“用户/活动”图标和文字“我的活动”。
- 已登录：进入现有“我的活动”页面。
- 未登录：进入现有登录流程，登录完成后进入“我的活动”或返回活动广场，具体沿用现有业务规则。
- 提供默认、悬浮、按下、键盘聚焦和禁用/加载状态。
- 不得仅使用图标，避免不同年龄和数字素养用户难以理解。

### 6.6 列表加载（P0）

- 保持当前滚动触底继续加载方式，批次大小由后端接口控制。
- 距离页面底部一定阈值时预加载下一批，避免用户滚动到空白区后才请求。
- 请求中在列表底部显示加载状态；首次加载使用卡片骨架屏。
- 防止同一游标重复请求和重复插入活动。
- 搜索或筛选变化时取消失效请求，避免旧结果覆盖新结果。
- 全部加载完成后显示“已加载全部活动”，不继续发起请求。
- 加载下一批失败时保留已加载内容，并提供“重新加载”按钮。

## 7. 三种视图的卡片布局

### 7.1 List View 列表视图（列表流 / 左图右文列表）

- 列表始终单列，不受 3/4/5/6 列规则影响。
- 单条活动为全宽横向卡片，不仅是“图片后跟一段文字”；使用明确的三分区结构，提高 1920 及以上宽屏的空间利用率。
- A 区“封面”：左侧固定比例封面，建议 240×135px，保持 16:9；在较窄 PC 宽度可降至 200×112.5px。
- B 区“核心信息”：展示置顶标签、活动名称、活动时间、活动标签，是用户快速比较活动的主要阅读区。
- C 区“补充信息”：展示自定义字段、主办方和活动地点；字段标签与字段值对齐，避免所有信息堆在标题下方。
- B 区占据剩余空间的主要部分，C 区建议宽 320–480px；超宽屏下 C 区不无限拉长，可通过区间留白保持阅读宽度。
- 状态、报名人数、浏览量可叠加于封面，但必须保证对比度。
- 每条最小高度由封面及 16px 上下内边距共同决定；内容超出时自然增高，不截断整项字段。
- 主办方、地点和自定义字段不应与时间使用相同视觉权重；字段名使用次要文字色，字段值使用正文色。
- 当补充字段较少时，C 区保持自然留白，不显示“单位”“暂无”等无意义占位。
- 当浏览器宽度小于 1366px 时，仍保持左图右文：A 区不变，C 区移动到 B 区下方形成右侧单列信息区，不得把整张活动卡片改为上下图文。

### 7.2 Card Grid 卡片网格布局（网格卡片流）

- 宫格卡片按行排列，每行遵循响应式列数。
- 同一行以最高卡片为行高，其余卡片拉伸至同高；卡片底部信息保持对齐。
- 卡片封面固定 16:9，使用 `object-fit: cover`。
- 卡片正文分为“标题区—时间区—标签/自定义字段区—机构地点区”，使用 8px 或 12px 的稳定垂直节奏。
- 置顶标签与标题同行；标题最多 2 行。置顶标签不得挤压标题至只剩极短文本，可在空间不足时独占标题首行。
- 时间最多 2 行；标签保持一行；自定义字段每项保持一行；主办方和地点位于卡片底部并各保持一行。
- 卡片正文使用纵向弹性布局，主办方与地点固定在正文底部区域；即使同一行卡片字段数量不同，机构地点区也应对齐。
- 完整字段卡片可以提高所在行的行高，但每个字段内部必须按行数规则省略，避免单个超长字段让整行卡片异常增高。
- 内容较短时允许留白，但不得用虚假字段填充。
- 留白应集中在正文中部，不能散落在多个空字段位置；底部区域必须保持稳定对齐。

### 7.3 Masonry View 瀑布流布局（瀑布卡片流）

- 卡片宽度与宫格视图一致，卡片高度由实际字段内容决定。
- 各列独立向下堆叠，不强制同行等高；纵向间距统一为 24px。
- 初始排布按接口返回顺序依次将卡片放入当前高度最短的列；列高相同时从左到右。
- 新加载活动继续按“最短列优先”追加，不重新打乱已展示卡片。
- DOM/数据顺序必须保持接口返回顺序，置顶与排序由后端决定，前端不得因布局修改业务排序。
- 图片加载前预留 16:9 空间，避免图片完成加载后造成明显布局跳动。
- 窗口跨断点变化时重新计算列数与位置；重排应节流，避免连续抖动。
- 卡片内字段规则、点击行为、状态样式与宫格视图完全一致。
- 与 Card Grid 的关键差异是“不强制同行等高”和“允许富信息自然展开”：标题可显示最多 3 行，标签可换至 2 行，主办方、地点及单个自定义字段可显示最多 2 行。
- 瀑布流不是任意图片比例的拼贴画；活动封面仍固定 16:9，卡片高度差主要来自真实字段数量和文本长度，保持活动平台的秩序感。
- 字段较少的卡片自然较短，字段较多的卡片自然较长；不得为对齐而填充空白，也不得为制造高度差随机裁切内容。

### 7.4 三种视图的字段展示映射（P0）

同一活动的数据完整性必须一致，但不同视图的阅读目的不同：List View 服务于快速横向比较，Card Grid 服务于封面驱动的均衡浏览，Masonry View 服务于富字段内容的紧凑探索。因此允许显示行数和信息分区不同，不允许擅自隐藏后台要求展示的字段。

| 字段 | List View 列表视图 | Card Grid 卡片网格 | Masonry View 瀑布流 |
|---|---|---|---|
| 活动封面 | 左侧 240×135px；<1366px 可缩至 200×112.5px | 顶部通栏，固定 16:9 | 顶部通栏，固定 16:9 |
| 活动状态 | 封面左上角 | 封面左上角 | 封面左上角 |
| 报名人数/浏览量 | 封面左下角，同组排列 | 封面左下角，同组排列 | 封面左下角，同组排列 |
| 置顶标签 | B 区标题前 | 标题前；不足时独占首行 | 标题前；不足时独占首行 |
| 活动名称 | B 区，最多 2 行 | 最多 2 行 | 最多 3 行 |
| 活动时间 | B 区，优先 1 行，空间不足最多 2 行 | 最多 2 行 | 最多 2 行，可完整展开更多日期信息 |
| 活动标签 | B 区，最多 3 个，其余“+N” | 1 行，最多 2 个，其余“+N” | 最多 2 行，建议最多 4 个，其余“+N” |
| 自定义字段 | C 区，按后台顺序全部渲染；每项 1 行 | 正文中段，按后台顺序全部渲染；每项 1 行 | 正文中段，按后台顺序全部渲染；每项最多 2 行 |
| 主办方 | C 区，1 行 | 底部机构区，1 行 | 底部机构区，最多 2 行 |
| 活动地点 | C 区，最多 2 行 | 底部地点区，1 行 | 底部地点区，最多 2 行 |

#### 7.4.1 共用视觉规则

- 卡片信息层级固定为：活动名称 > 活动时间 > 决策信息（标签/主办方/地点） > 自定义补充字段 > 浏览统计。
- 活动状态是生命周期信息，置顶是运营信息，标签是内容分类，三者必须使用不同样式和位置，不能混成同一组彩色标签。
- 封面上的状态与统计信息使用底部/局部渐变遮罩，不使用覆盖整张封面的深色蒙层。
- 图标尺寸、字段行高和标签高度在三种视图间保持一致，确保切换时仍有同一产品的连续感。
- 缺失的可选字段整项收起，后续字段自然前移；不得保留空行、破折号或“单位”等模糊占位。
- 省略号只用于单个字段的视觉截断；不得把多个字段合并后整体截断，避免用户不知道缺失的是哪类信息。
- 被省略的文本应通过原生标题提示或无障碍说明提供完整内容；触屏设备不能依赖悬浮提示才能理解核心信息。

### 7.5 List View 内部响应式规则（P0）

| 浏览器宽度 | 列表卡片内部结构 |
|---|---|
| <1366px | A 区封面 + 右侧信息；B、C 两区在右侧上下排列 |
| 1366–1919px | A/B/C 三分区；C 区约 320px |
| 1920–2299px | A/B/C 三分区；C 区约 360–420px |
| ≥2300px | A/B/C 三分区；C 区最大约 480px，其余空间用于区间留白，不拉长单行阅读距离 |

- List View 的单列规则是“结果项一行一个”，不代表卡片内部只能有一列文字。
- 当全部字段存在时，卡片允许随内容增高；当字段较少时，卡片不得低于封面和内边距形成的最小高度。
- B、C 区之间建议使用 24–40px 间距，不建议使用明显竖分割线制造表格感。

### 7.6 不同布局下的美观性判断标准

- List View：重点检查宽屏是否仍有大片无意义空白、字段是否形成清晰分区、连续浏览时标题和封面是否对齐。
- Card Grid：重点检查同一行卡片底部是否对齐、完整字段是否导致异常高行、短卡片留白是否集中且有秩序。
- Masonry View：重点检查各列高度是否均衡、长短卡片是否自然、是否出现重叠、顺序跳跃或过度参差。
- 三种视图：重点检查同一字段的图标、标签、字号和语义是否一致，切换后用户能否立即建立对应关系。

## 8. 活动卡片字段规范

### 8.1 字段优先级与展示规则

| 序号 | 字段 | 必填/可选 | 展示位置与规则 |
|---|---|---|---|
| 1 | 活动封面 | 必填 | 卡片顶部/左侧，16:9；无图时使用平台统一占位图，不拉伸低清图片 |
| 2 | 状态 | 必填 | 封面左上角；显示文字标签，不仅靠颜色 |
| 3 | 置顶状态 | 可选 | `isTop=true` 时在标题前显示“置顶”标签；未置顶不占位 |
| 4 | 活动名称 | 必填 | 卡片正文最高层级；List View、Card Grid 最多 2 行，Masonry View 最多 3 行；完整名称可通过标题属性/详情查看 |
| 5 | 活动时间 | 必填 | 标题下方；按 7.4 映射控制行数，不省略关键日期或时间 |
| 6 | 报名人数 | 可选 | 封面底部信息层；仅有当前人数时显示 N，有名额上限时显示 N/M |
| 7 | 浏览量 | 可选 | 与报名人数同组，使用浏览图标 + 数值 |
| 8 | 活动标签 | 可选 | 根据视图展示 2–4 个，超出显示“+N”，具体见 7.4；点击是否筛选由后续版本决定，本期默认不可点击 |
| 9 | 主办方 | 可选 | List View 位于 C 区，卡片类视图位于正文底部；格式“主办方：名称”；无数据时整行隐藏，不显示“单位”等占位文本 |
| 10 | 活动地点 | 可选 | List View 位于 C 区，卡片类视图位于正文底部；地点图标 + 文本；线上活动可显示“线上活动”或平台名称 |
| 11 | 自定义字段 | 可选 | List View 位于 C 区，卡片类视图位于时间与机构地点区之间；按后台配置控制显隐和顺序，默认不展示 |

### 8.2 状态规范

目标展示状态包括：

- 未开始：活动尚未开始。
- 报名中：活动处于可报名状态。
- 进行中：活动已经开始且尚未结束。
- 已结束：活动已结束。

状态颜色建议采用语义 token：未开始使用蓝色、报名中使用橙色、进行中使用绿色、已结束使用中性灰。文字与背景对比度需满足可访问性要求。

> 待确认：线上筛选当前使用“即将开始”，而卡片需求使用“未开始”；“报名中”是否与活动生命周期互斥也需由后端状态模型确认。前端只渲染后端最终状态，不自行推导或覆盖状态。

### 8.3 置顶规则

- 已置顶活动显示“置顶”标签；未置顶不显示空标签。
- 置顶标签与活动状态分开，避免用户将“置顶”误解为状态。
- 活动排序以接口返回为准，前端不二次排序。

### 8.4 自定义字段规则

- 默认不展示任何自定义字段。
- 后台配置某字段为前台可见后，前端按配置顺序展示“字段名：字段值”。
- 字段名、字段值均需转义，防止脚本或富文本污染卡片。
- 空值字段不展示。
- 单个字段在 List View 和 Card Grid 中默认最多 1 行；Masonry View 中最多 2 行，具体以 7.4 为准。
- 前端必须渲染所有被配置为可见且有值的字段；建议后台限制单张卡片最多配置 3 个前台字段，避免信息失控。

### 8.5 文本与数字格式

- 日期时间使用 24 小时制；同一页面保持统一格式。
- 跨年活动须显示完整年份；同年活动可在不引起歧义时简写结束年份。
- 人数、浏览量超过 9,999 时可缩写为 1万、1.2万，并通过悬浮提示提供完整值。
- 标签和机构名称不得用字符截断破坏含义；视觉省略使用 CSS 省略号。
- 页面语言默认简体中文，所有固定文案进入国际化资源，不硬编码在组件内。

### 8.6 卡片交互

- 整张卡片可点击进入活动详情；标题使用可聚焦链接语义。
- 卡片默认在当前标签页打开；沿用现有业务跳转和权限校验。
- 悬浮态仅提供轻微边框/阴影变化，不使用明显上浮或缩放。
- 键盘聚焦态必须清晰可见；按 Enter 可进入详情。
- 封面图 `alt` 使用活动名称；纯装饰图标对读屏隐藏。

## 9. 响应式布局规范（P0）

### 9.1 固定规则

- 左右内边距在所有 PC 宽度下始终为 72px，不使用随宽度变化的外边距。
- Card Grid 卡片网格布局与 Masonry View 瀑布流布局使用相同列数规则。
- List View 列表视图始终单列。
- 列间距固定为 24px。
- 断点按 CSS 布局视口宽度判定，使用 `min-width` 媒体查询实现。

### 9.2 断点与列数

| 浏览器宽度 W | Card Grid / Masonry View 列数 | CSS 条件 |
|---|---:|---|
| W < 1366px | 3 列 | 默认规则 |
| 1366px ≤ W ≤ 1919px | 4 列 | `@media (min-width: 1366px)` |
| 1920px ≤ W ≤ 2299px | 5 列 | `@media (min-width: 1920px)` |
| W ≥ 2300px | 6 列 | `@media (min-width: 2300px)` |

### 9.3 宽度计算

```text
内容区宽度 C = 浏览器宽度 W - 72 × 2
单卡宽度 K = [C - 24 × (列数 N - 1)] ÷ N
```

关键宽度参考：

| 浏览器宽度 | 内容区宽 | 列数 | 单卡理论宽度 |
|---:|---:|---:|---:|
| 1024px | 880px | 3 | 277.33px |
| 1365px | 1221px | 3 | 391px |
| 1366px | 1222px | 4 | 287.5px |
| 1919px | 1775px | 4 | 425.75px |
| 1920px | 1776px | 5 | 336px |
| 2299px | 2155px | 5 | 411.8px |
| 2300px | 2156px | 6 | 339.33px |

- 工程实现建议使用 CSS Grid：`grid-template-columns: repeat(N, minmax(0, 1fr))`。
- 所有卡片使用 `box-sizing: border-box`，小数像素由浏览器分配，不手工取整造成末列错位。
- 断点切换时出现卡片宽度回落属于指定规则的正常结果，不能通过延迟断点规避。
- 建议本期 PC 最小支持宽度为 1024px；低于 1024px 时仍保持 3 列和 72px 边距，并允许页面横向滚动。该最小宽度需在评审中确认。

### 9.4 区域自适应

- 头部：搜索框可在 360–480px 内流动；“我的活动”保持完整文字。
- 筛选区：维度名称列固定，选项区自动换行；不得通过横向滚动展示筛选项。
- 结果工具栏：不足时可换行，结果数量优先位于首行左侧，报名筛选与视图切换保持同组。
- 卡片：封面比例固定，正文宽度变化；标题和元数据按最大行数省略，不缩小到不可读字号。

## 10. UI 视觉规范

### 10.1 设计原则

- 内容优先：封面吸引注意，标题和时间帮助判断，主办方/地点完成决策。
- 信息分层：状态、置顶、标签使用不同语义，不堆叠成同一视觉层级。
- 克制清晰：避免大面积高饱和装饰、过重阴影和不必要动效。
- 跨人群可用：文案直白，点击区域充足，不能依赖熟悉图标或仅靠颜色理解。

### 10.2 建议尺寸

| 元素 | 建议规格 |
|---|---|
| 页面标题 | 24px/32px，字重 600 |
| 卡片标题 | 16px/24px，字重 600 |
| 筛选维度名 | 14px/22px，字重 500 |
| 正文/选项 | 14px/22px |
| 次要信息 | 13px/20px，不低于 12px |
| 搜索框/常规按钮 | 高 40px |
| 视图切换按钮 | 40×40px |
| 状态/置顶标签 | 高 24px，水平内边距 8px |
| 卡片圆角 | 12px |
| 控件圆角 | 8px |
| 卡片正文内边距 | 16px |
| 卡片列/行间距 | 24px |

### 10.3 语义颜色

- 使用平台现有品牌主色作为搜索聚焦、选中态、链接和主要操作色。
- 页面背景、卡片背景、边框、主文字、次要文字均使用语义 token，不在业务组件散落硬编码色值。
- 状态颜色按 8.2 约定；所有浅色标签文字与背景组合须达到至少 4.5:1 的文字对比度。
- 封面上的白色文字/图标下方增加渐变遮罩，确保不同图片上均清晰可见。

### 10.4 层级与反馈

- 卡片默认使用 1px 中性边框或极轻阴影；悬浮时增强边框/阴影，不明显位移。
- 输入框聚焦、按钮聚焦、卡片链接聚焦均显示 2px 可见焦点环。
- 所有点击操作需在 100ms 内提供视觉反馈。
- 页面加载不使用全屏遮罩阻断已加载内容。

## 11. 页面状态设计

### 11.1 首次加载

- 页面头部和筛选区先显示；结果区展示与当前列数一致的一行骨架卡片。
- 骨架卡片结构模拟封面、标题、时间和底部信息，不使用快速闪烁动画。

### 11.2 搜索/筛选刷新

- 保留页面结构和当前条件，结果区切换为轻量骨架或局部加载状态。
- 请求完成后更新结果数量和列表。
- 避免先清空列表导致页面高度骤降；可保留旧内容并降低透明度，但不可继续点击旧结果。

### 11.3 空结果

- 文案：“未找到符合条件的活动”。
- 辅助说明：“请尝试调整关键词或筛选条件”。
- 提供“清除全部条件”按钮。
- 如只有“我能报名”导致空结果，可提供“查看全部活动”。

### 11.4 首次加载失败

- 明确说明加载失败，不显示空结果文案。
- 提供“重新加载”按钮。
- 保留搜索和筛选条件。

### 11.5 继续加载失败

- 已加载卡片保持可用。
- 列表底部显示“加载失败，点击重试”。
- 重试使用同一游标，防止重复数据。

### 11.6 无封面/字段缺失

- 无封面：使用统一活动占位图。
- 无可选字段：整行隐藏，不留空白标签。
- 必填字段异常：使用不误导用户的兜底文案，并上报数据质量日志。

## 12. 可访问性与包容性（P0）

- 目标达到 WCAG 2.1 AA 的核心要求。
- 页面必须存在唯一一级标题“活动广场”。
- 搜索、日期、积分输入均有显式标签或等价可访问名称。
- 所有操作可通过键盘完成；Tab 顺序与视觉阅读顺序一致。
- 视图切换、复选框、筛选选项必须暴露选中状态。
- 状态、错误、选中态不能仅靠颜色表达。
- 正文文字对比度至少 4.5:1，大字号至少 3:1。
- 控件点击区域建议不小于 40×40px；相邻控件保留足够间距。
- 动态更新结果数量时使用温和的 `aria-live`，避免每次卡片加载均重复播报。
- 卡片封面使用活动名称作为替代文本；装饰性图标对读屏隐藏。
- 200% 页面缩放下核心功能可用，内容不得重叠或被裁切。

## 13. 性能、兼容性与安全

### 13.1 性能

- 首屏封面按实际显示宽度提供响应式图片，优先支持 WebP/AVIF 并保留兼容格式。
- 非首屏图片懒加载，首屏关键图片不得全部延迟加载。
- 图片区域预留尺寸，页面累计布局偏移 CLS 目标 ≤ 0.1。
- 建议在约定测试环境中达到 LCP ≤ 2.5s、INP ≤ 200ms。
- 瀑布流重排、窗口 resize 和滚动监听必须节流；优先使用 `ResizeObserver`/`IntersectionObserver`。
- 长列表应监控 DOM 数量；数据规模较大时评估虚拟列表，但不得破坏浏览器查找、读屏或返回定位。

### 13.2 浏览器

- 支持主流桌面版 Chrome、Edge、Firefox、Safari 的当前及前一个主要版本。
- 不支持 IE。
- 在 Windows 和 macOS 至少各完成一次视觉与键盘回归。

### 13.3 安全

- 活动名称、标签、机构、地点和自定义字段均按纯文本转义。
- 搜索参数和筛选参数由后端再次校验，前端校验不能替代服务端校验。
- 跳转 URL 需经过现有安全规则处理，不允许未校验的 `javascript:` 或开放重定向。

## 14. 数据与接口要求

### 14.1 活动卡片最小数据结构（示意）

```json
{
  "id": "activity_id",
  "title": "活动名称",
  "coverUrl": "https://...",
  "status": "not_started | registering | ongoing | ended",
  "isTop": true,
  "startTime": "2026-08-14T09:00:00+08:00",
  "endTime": "2026-08-20T18:00:00+08:00",
  "signupCount": 120,
  "signupCapacity": 200,
  "viewCount": 3560,
  "tags": ["音乐", "舞蹈", "武术"],
  "organizer": "主办方名称",
  "location": "活动地点",
  "online": false,
  "customFields": [
    {
      "key": "coOrganizer",
      "label": "协办方",
      "value": "某某单位",
      "visible": false,
      "order": 1
    }
  ],
  "detailUrl": "https://..."
}
```

### 14.2 列表接口要求

- 返回活动数组、总数、下一页游标/页码、是否还有更多。
- 筛选枚举建议由独立配置接口返回，避免前端硬编码后台可配置数据。
- 接口明确状态枚举、时间时区、人数上限为空时的含义。
- 相同条件和游标返回稳定顺序，避免继续加载时重复或遗漏。
- 自定义字段只返回允许前台展示的字段，前端仍按 `visible` 二次判断。

## 15. 埋点建议（P1）

| 事件 | 触发时机 | 关键参数 |
|---|---|---|
| `activity_square_search` | 提交搜索 | keyword_length、has_filters、result_count |
| `activity_square_filter_change` | 筛选变化 | filter_dimension、filter_value、result_count |
| `activity_square_filter_reset` | 重置筛选 | reset_scope |
| `activity_square_view_switch` | 切换视图 | from_view、to_view、viewport_width、column_count |
| `activity_square_eligible_only` | 切换“我能报名” | checked、login_status |
| `activity_square_card_click` | 点击卡片 | activity_id、position、view_mode、is_top、status |
| `activity_square_my_activity_click` | 点击我的活动 | login_status |
| `activity_square_load_more` | 触发继续加载 | batch_index、success、duration |

- 埋点不得采集完整搜索关键词等可能包含个人信息的内容；仅采集必要长度、是否为空或经批准的脱敏分类。

## 16. 验收标准

### 16.1 核心功能

- [ ] 搜索可通过 Enter 和按钮触发，清空后恢复结果。
- [ ] 7 个筛选维度均可选择、组合、重置，结果总数正确更新。
- [ ] “我能报名”按登录状态正确处理，并保留页面条件。
- [ ] “我的活动”入口在已登录/未登录场景均符合现有业务流程。
- [ ] List View、Card Grid、Masonry View 三种视图均可切换。
- [ ] 切换视图不丢失筛选、搜索、已加载数据和业务排序。
- [ ] 卡片 11 类字段按配置正确展示，空的可选字段不占位。
- [ ] 同一条“全部字段”活动在三种视图中均按 7.4 映射展示，未擅自丢弃配置为可见的字段。
- [ ] List View 在 1920 及以上宽屏采用 A/B/C 分区，补充信息不再全部堆叠在左侧标题下方。
- [ ] Card Grid 同行卡片底部信息区对齐，长字段不会无限拉高整行。
- [ ] Masonry View 允许富字段自然展开，不使用 Grid 的等高行规则。
- [ ] 置顶标签、状态标签、报名人数、浏览量语义正确。
- [ ] 滚动加载无重复、无遗漏，失败可重试。

### 16.2 响应式

- [ ] 1365px 宽时 Card Grid 和 Masonry View 均为 3 列。
- [ ] 1366px、1919px 宽时均为 4 列。
- [ ] 1920px、2299px 宽时均为 5 列。
- [ ] 2300px 及以上均为 6 列。
- [ ] 各宽度左右内边距均为 72px，列间距均为 24px。
- [ ] List View 在所有 PC 宽度下始终单列，并保持左图右文结构。
- [ ] 筛选项可换行，无内容重叠、横向裁切或不可点击区域。
- [ ] 200% 缩放下核心功能仍可操作。

### 16.3 Masonry View 瀑布流布局

- [ ] 卡片按真实内容高度展示，不强制同行等高。
- [ ] 卡片按最短列优先放置，列高相同时从左到右。
- [ ] 新数据追加后不改变已展示卡片的业务顺序。
- [ ] 图片加载和窗口重排时无明显跳动、闪烁或重叠。
- [ ] 断点切换后列数准确，卡片不超出内容区。

### 16.4 状态与可访问性

- [ ] 首次加载、刷新、空结果、首次失败、继续加载失败、全部加载完成均有明确状态。
- [ ] 三种视图切换、筛选、复选框和卡片均可键盘操作。
- [ ] 焦点态清晰，状态/错误不只依赖颜色。
- [ ] 卡片图片替代文本、按钮名称、选中状态可被读屏识别。
- [ ] 关键文字和控件对比度符合 WCAG 2.1 AA。

## 17. 测试矩阵

| 类型 | 必测值/场景 |
|---|---|
| 视口宽度 | 1024、1365、1366、1600、1919、1920、2299、2300、2560、3840 |
| 视图 | List View（左图右文列表）、Card Grid（卡片网格）、Masonry View（瀑布流） |
| 字段 | 三种视图分别测试最少字段、全部字段、超长标题、超长主办方/地点、多自定义字段、无封面 |
| 状态 | 未开始、报名中、进行中、已结束、置顶/未置顶 |
| 结果 | 有结果、零结果、首次失败、继续加载失败、全部加载完成 |
| 登录 | 已登录、未登录、登录后返回 |
| 输入 | 键盘、鼠标、触屏 PC、200% 缩放、减少动态效果 |
| 浏览器 | Chrome、Edge、Firefox、Safari 当前及前一主要版本 |

## 18. 优先级与建议迭代

### 第一阶段：需求与视觉定稿

- 确认状态模型、搜索范围、区域字段关系和最小支持宽度。
- 输出 1920 标准稿，以及 1365/1366/1919/1920/2299/2300 关键响应式标注。
- 输出三种视图、全字段卡片、空/加载/异常状态。

### 第二阶段：P0 开发

- 完成头部、筛选、结果工具栏和卡片视觉优化。
- 接入三种视图，新增瀑布流。
- 完成响应式、加载状态、错误恢复和基础可访问性。

### 第三阶段：P1 增强

- 保存视图偏好。
- 增加筛选摘要。
- 完善埋点与数据看板。
- 根据真实数据评估长列表虚拟化。

## 19. 上线前待确认项

以下事项会影响最终设计或开发验收，应在视觉稿冻结前确认：

1. “即将开始”与“未开始”是否为同一状态；“报名中”是否为独立维度或互斥状态。
2. 搜索最终覆盖活动名称，还是同时覆盖标签、主办方、地点及自定义字段。
3. “区域”和“所属区域”的业务定义、级联关系及是否允许多选。
4. PC 最小支持宽度是否确定为 1024px；低于该宽度是否允许横向滚动。
5. 三种视图的默认值和偏好保存范围（当前会话、浏览器长期保存或账号同步）。
6. 继续加载的接口形式、单批数据量及是否保留当前滚动加载方式。
7. 自定义字段的最大配置数量、单字段长度和跨视图展示行数。
8. 报名人数是否存在名额上限，以及 `N/M` 中 M 为空、0、无限制时的准确含义。
9. 线上活动地点的统一文案和时间时区规则。
10. 未登录用户点击“我能报名”“我的活动”和活动详情后的返回路径。

## 20. 交付物要求

后续 UI 设计至少应包含：

- 1920px 标准画布完整页面。
- 1365、1366、1919、1920、2299、2300 六个断点临界状态。
- List View（列表流 / 左图右文列表）、Card Grid（网格卡片流）、Masonry View（瀑布卡片流）三种视图。
- 普通卡片、置顶卡片、全字段卡片、最少字段卡片、长文本卡片。
- 未开始、报名中、进行中、已结束四种状态。
- 默认、悬浮、按下、键盘聚焦、选中、禁用状态。
- 首次加载、局部刷新、空结果、首次失败、继续加载失败、全部加载完成。
- 组件、间距、字号、颜色、圆角、阴影和响应式标注。

---

### 文档维护约定

- 本文档保存在项目工作区，作为跨对话可读取的长期记忆文件。
- 后续每次确认待定项、调整断点、改变字段或交互，都应先更新版本号和变更记录。
- 视觉稿、原型和前端实现若与本文档冲突，以最新评审通过版本为准，并在本文档记录差异。
